---
- name: install apache in apps
  hosts: db
  become: true
  vars:
    mysql_databases:
      - name: webdb
        collation: utf8_general_ci
        encoding: utf8
        replicate: 1
  roles:
    - greenlingguy.mysql    
...